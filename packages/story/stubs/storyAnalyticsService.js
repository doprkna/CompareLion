/** @parel/story stub - build resolution only. @parel/story excluded from build:vercel. */
const STUB_MSG = '@parel/story stub: story not built. Replace with real implementation.';

export async function incrementStoryView(storyId, userId) {
  throw new Error(STUB_MSG);
}

export async function getStoryAnalytics(storyId) {
  throw new Error(STUB_MSG);
}
