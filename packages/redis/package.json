{
  "name": "@parel/redis",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "node -e \"console.log('cwd',process.cwd()); console.log('node',process.version); try{console.log('resolve ioredis ->', require.resolve('ioredis'))}catch(e){console.error('resolve ioredis FAILED', e.message)}\" && tsc -b"
  },
  "dependencies": {
    "ioredis": "^5.7.0"
  }
}

