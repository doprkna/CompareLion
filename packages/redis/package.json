{
  "name": "@parel/redis",
  "version": "0.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "build": "node -e \"console.log('cwd',process.cwd()); console.log('node',process.version); console.log('resolve ioredis ->', require.resolve('ioredis'))\" && tsc -b"
  },
  "dependencies": {
    "ioredis": "^5.7.0"
  }
}

