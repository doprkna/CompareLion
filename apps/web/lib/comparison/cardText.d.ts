export declare function buildFunText(username: string, region: string, stats: {
    metric: string;
    valuePct: number;
}): string;
export declare function renderSvgCard(funText: string, bigNumber: string, bg?: string, fg?: string): string;
