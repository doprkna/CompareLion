/**
 * Notification Bell Component
 * Shows notification count and opens notification panel
 * TODO: Notifications loader temporarily disabled in dev — apiFetch returns { ok, data } (parsed), not Response; on failure render nothing, no throw/log.
 * v0.40.17 - Story Notifications 1.0
 */
export default function NotificationBell(): import("react").JSX.Element | null;
