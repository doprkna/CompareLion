export type Achievement = {
  id: string;
  title: string;
  description: string;
  icon: string; // placeholder, graphic will replace later
  condition: string; // human-readable condition for now
};
