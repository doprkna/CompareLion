/**
 * Minimal placeholder for region/language selector.
 * TODO: Implement proper RegionSelector (original was in archive/dead-code).
 * Renders nothing to avoid runtime errors on app/main (post-login).
 */
export default function RegionSelector(): null;
