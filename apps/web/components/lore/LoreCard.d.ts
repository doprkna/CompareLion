import { LoreEntry } from '@parel/core/hooks/useLore';
interface LoreCardProps {
    entry: LoreEntry;
}
export declare function LoreCard({ entry }: LoreCardProps): import("react").JSX.Element;
export {};
