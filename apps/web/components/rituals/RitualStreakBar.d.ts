interface RitualStreakBarProps {
    streakCount: number;
    totalCompleted: number;
    maxStreak?: number;
}
export declare function RitualStreakBar({ streakCount, totalCompleted, maxStreak, }: RitualStreakBarProps): import("react").JSX.Element;
export {};
